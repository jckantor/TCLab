# Store the version here so:
# 1) we don't load dependencies by storing it in __init__.py
# 2) we can import it in setup.py without loadiing dependencies
# 3) we can import it into your module

__version__ = "0.4.10dev"
